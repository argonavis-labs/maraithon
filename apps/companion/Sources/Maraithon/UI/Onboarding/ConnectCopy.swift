import Foundation

/// User-facing copy for the first pairing screen. The copy should set a
/// clear expectation for the local sources the Mac companion can sync.
enum ConnectCopy {
    static let title = "Connect to Maraithon"
    static let body = "Pair this Mac so Maraithon can answer with context from Messages, Notes, Voice Memos, Calendar, Reminders, files, and browser history. Everything is encrypted; you can revoke this device any time."
    static let connectButton = "Connect"
}
